name: safe_step_app  # Changed from 'safe step'
description: A Public Safety Application for Bangladesh.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  firebase_storage: ^11.5.6
  firebase_messaging: ^14.7.9
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  http: ^1.1.0 
  shake: ^2.2.0 
  speech_to_text: ^6.6.0 
  image_picker: ^1.0.4 
  camera: ^0.10.5+5
  path_provider: ^2.1.1
  provider: ^6.1.1
  flutter_spinkit: ^5.2.0 
  webview_flutter: ^4.4.2
  webview_flutter_android: any
  url_launcher: ^6.2.1
  google_maps_flutter: ^2.5.0
  flutter_background_service: ^5.0.0
  supabase_flutter: ^2.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/